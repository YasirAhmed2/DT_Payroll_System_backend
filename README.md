# express-backend

Express + PostgreSQL payroll backend.

## Run

1. Copy `.env.example` to `.env`.
2. Paste your PostgreSQL connection string into `DATABASE_URL`.
3. Install dependencies:

```bash
npm install
```

4. Start the server:

```bash
npm start
```

The API is exposed under `/api` and matches the existing frontend contract.
